
pip freeze: A built in python command used to output a clean, text-based list of every single external library currently installed in your active python environment, along with their exact version.

use `pip freeze > requirements.txt` to get copy of python environment in requirements.txt file.